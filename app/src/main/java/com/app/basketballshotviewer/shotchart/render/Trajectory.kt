package com.app.basketballshotviewer.shotchart.render

/* accept arguments like duration, timestamp, angles, etc*/
class Trajectory(private val arg: Map<String, Int>) {

    fun draw(){
    }

    /* delete the GL buffer after trajectory creation*/
    fun dispose(){
    }
}