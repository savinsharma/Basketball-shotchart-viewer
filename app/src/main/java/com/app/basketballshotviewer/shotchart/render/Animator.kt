package com.app.basketballshotviewer.shotchart.render

/*
* An animation coordinator in render converts events to keyframes or trajectories.
* */
class Animator(private val maxArc: Int, private val stepsPerArch: Int) {

    /* Sync with latest UI state from view model */
    fun sync(){
    }
    fun draw(){
    }
}
